import React from 'react';

const SpectateView: React.FC = () => {
  const viewName = 'Spectate view';

  return (
    <div>
      <h1>{viewName}</h1>
    </div>
  );
};

export default SpectateView;
